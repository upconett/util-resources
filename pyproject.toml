[project]
name = "resources"
version = "0.1.0"
description = "Small library for managing resources"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.3.5",
    "python-dotenv>=1.1.0",
    "pyyaml>=6.0.2",
]
